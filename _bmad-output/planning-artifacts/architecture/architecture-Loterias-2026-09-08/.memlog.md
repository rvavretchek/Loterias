---
scope: Arquitetura tecnica para as duas features da PRD Loterias 2026-09-07 (verificacao/notificacao diaria de resultados via django-crontab; novo fluxo de cadastro email->link->senha->nome) mais a renomeacao de identificadores de codigo legado do portugues para o ingles decidida durante a revisao dessa PRD
purpose: Fixar as invariantes tecnicas (paradigma, fronteiras, jobs agendados, migrations seguras) antes de quebrar em epics/stories
altitude: feature
updated: 2026-09-08T02:08
---

- (event) Run iniciado a partir da PRD prd-Loterias-2026-09-07/prd.md (finalizada) e do CLAUDE.md do repo. Inputs: PRD (2 features + convencao de nomenclatura de codigo em ingles com renomeacao de legado, secao 3.1), docs/diagnostico-projeto.md (removal da multitenancy), CLAUDE.md (stack Django 5.0.6/allauth 0.63.3, SQLite unico, deploy Docker+gunicorn via docker-compose lab).
- (direction) Modo: Coaching. Entregaveis: ARCHITECTURE-SPINE.md (build substrate) + doc explicativo (HTML/md) para o Boss revisar com calma / peca de portfolio.
- (constraint) [ADOPTED] Codigo legado atual (models.py, utils.py, views.py) investigado: GeneratedBet(JogoGerado) guarda campos-cache result_checked/hits/prize/prize_description atualizados on-demand por verificar_resultado_jogo/salvar_jogo_manual/verificar_resultados_usuarios; LotteryResult(ResultadoLoteria) ja existe e ja e escrito via update_or_create em 2 pontos (view manual e view de verificacao); detalhes_jogo recalcula premio_info ao vivo comparando GeneratedBet x LotteryResult toda vez que a pagina carrega, redundante com os campos-cache. Nao ha job agendado hoje - toda verificacao e sob demanda (clique).
- (decision) HitNotification e NotificationPreference vivem em apps/loterias_core (mesmo app de GeneratedBet/LotteryResult/GameStatistics), nao em app novo. Motivo do Boss: projeto solo de pequeno porte, FK local sem dependencia cruzada entre apps. Previne: um builder criar apps/notifications e outro assumir loterias_core, gerando import circular ou app duplicado.
- (decision) Campos-cache de GeneratedBet (result_checked, hits, prize, prize_description) continuam sendo a fonte que as telas existentes (historico, detalhes_jogo) leem; a rotina diaria (fetch_daily_results) escreve LotteryResult + HitNotification E TAMBEM atualiza esses campos-cache no GeneratedBet correspondente, replicando a logica que check_bet_result_view/save_manual_bet_view ja fazem hoje. HitNotification nao substitui os campos-cache, e a camada de aviso/leitura em cima do mesmo fato. Previne: duas fontes de verdade divergentes (GeneratedBet dizendo 'sem premio' enquanto HitNotification diz 'premiado').
- (decision) Retry de FR-9 implementado como 3 entradas de crontab independentes (3h00, 3h15, 3h30), nao como sleep bloqueante dentro de um unico job. Cada execucao roda a mesma funcao idempotente fetch_daily_results, que so tenta concursos ainda sem LotteryResult - a 2a/3a chamada naturalmente so re-tenta o que falhou. O alerta ao operador (FR-9) so dispara na execucao das 3h30 se o concurso ainda estiver sem resultado. Previne: um builder implementar sleep(900) dentro do processo de cron, bloqueando o worker por ate 30min e travando outros jobs agendados no mesmo processo.
- (decision) Renomeacao do codigo legado (PRD prd.md paragrafo 3.1: JogoGerado->GeneratedBet, ResultadoLoteria->LotteryResult, EstatisticaJogo->GameStatistics, capturar_resultado_cef->fetch_cef_result, etc, incluindo model fields) roda como epic isolado ANTES de qualquer FR novo (FR-1 a FR-14), usando Django RenameModel/RenameField migrations (seguras no SQLite moderno - rename via ALTER TABLE RENAME COLUMN/TABLE quando a versao do SQLite suporta, senao Django reconstroi a tabela preservando dados; nenhuma das duas formas perde dados). So depois da suite de testes verde com os nomes novos e que HitNotification/NotificationPreference/fluxo de cadastro comecam a ser construidos - jah nascem 100% em ingles, sem misturar convencao no meio do caminho. Previne: PRs concorrentes escolhendo nomes diferentes pro mesmo simbolo legado, e um mapeamento de \u00a73.1 parcialmente aplicado por varios sprints.
- (decision) As rotinas agendadas (fetch_daily_results 3x/dia, update_monthly_prize_values 1x/mes) rodam num servico sidecar novo no docker-compose (ex. loterias-cron), mesma imagem/Dockerfile do loterias-web mas CMD diferente (instala os crontabs via manage.py crontab add e roda cron em foreground), compartilhando o mesmo volume loterias_data (SQLite) e as mesmas env vars de EMAIL/SECRET_KEY. loterias-web continua rodando so gunicorn, sem mudanca. Motivo: Dockerfile atual (python:3.12-slim) nao tem daemon de cron instalado e o container so roda gunicorn como processo unico - sem esse servico novo, django-crontab nunca dispara nada em producao apesar de estar no requirements.txt. Previne: achar que 'add django-crontab e CRONJOBS no settings' basta, e descobrir em producao que a rotina diaria nunca rodou.
- (constraint) [ADOPTED da PRD] django-crontab e a escolha de agendamento (nao Celery/Redis, que serao removidos do requirements.txt); envio de e-mail via SMTP Brevo ja configurado, sincrono, sem fila.
- (event) Espinha distilada e escrita em ARCHITECTURE-SPINE.md (8 ADs). Lint automatico (lint_spine.py) passou sem findings. Disparados 3 reviewers em paralelo (subagentes): rubric walker (checklist de boa espinha), tech-currency (versoes: django-crontab, SQLite rename support, Python 3.11 sqlite3), adversarial (duas unidades cumprindo os ADs a risca mas divergindo). Aguardando resultados antes de Finalize.
- (event) 3 reviewers voltaram. Rubric walker: not-ready (2 HIGH, 1 medium-high, 1 medium, 2 low). Tech-currency: mostly accurate (1 medium sobre subestimar a defasagem do django-crontab). Adversarial: not-safe-to-split (2 CRITICAL, 3 HIGH/medium-high, 2 medium) - G1 (HitNotification acoplado a quem escreveu LotteryResult, silenciosamente suprime notificacao de outros usuarios), G2 (allowlist do gate de perfil sem excecao de admin/staff e sem grandfather, tranca todo usuario existente incl. o operador no deploy), G3 (tela de criar-senha nao esta na allowlist, gate de nome intercepta antes), G4 (sem unique constraint em HitNotification, cron overlapping pode duplicar email), G5 (nada diz como fetch_daily_results sabe que e a tentativa final/3h30), G6 (mapeamento de rename da PRD paragrafo 3.1 nao cobre related_name e JOGOS_CHOICES compartilhado entre classes), G7 (2 containers escrevendo no mesmo SQLite sem timeout/WAL).
- (decision) Fixes aplicados na espinha apos o gate: AD-2 fortalecido (rename e 1 PR unico, nao dividido por model, cobre related_name/JOGOS_CHOICES); AD-3 esclarece OneToOneField+defaults de NotificationPreference; AD-4 reescrito - cobertura de HitNotification agora e derivada do estado (todo GeneratedBet com LotteryResult correspondente sem HitNotification, nao so o que fetch_daily_results escreveu nesta chamada) + HitNotification.bet OneToOneField com get_or_create (fecha G1 e G4); AD-5 acrescenta fetch_daily_results(final=bool) e --final no command; AD-6 amarra --final a entrada das 3h30; AD-7 acrescenta apt-get install cron+tzdata no Dockerfile, TZ=America/Sao_Paulo e restart:unless-stopped no sidecar, e DATABASES OPTIONS timeout=20 (fecha rubric#1, rubric#3, rubric#6b, G7); AD-8 reescrito como middleware unico com precedencia explicita, excecao is_staff/is_superuser/admin, e campo novo User.profile_completed com migration de dados fazendo grandfather dos usuarios existentes (fecha G2, G3); AD-9 novo cobre a inversao dos 3 settings do allauth (ACCOUNT_SIGNUP_FIELDS, ACCOUNT_LOGIN_ON_EMAIL_CONFIRMATION) e set_unusable_password() como unica representacao de conta pendente (fecha rubric#2). Stack: wording do risco de django-crontab corrigido (desde 2016, nao '12+ meses'). FR-8 'tabela de valores de premiacao vigentes' nao modelada - registrada em Deferred como questao de volta pra PRD, nao inventada.
- (event) lint_spine.py OK apos os fixes (0 findings). Restam: G6 exige tambem atualizar a PRD paragrafo 3.1 (related_name, JOGOS_CHOICES) - tratado como atualizacao da fonte, nao so da espinha.
- (event) spine finalized
- (decision) 2026-09-08, pos-finalize durante criacao de epics/stories: Boss resolveu a ambiguidade de FR-8 (registrada em Deferred). Novo AD-10: PrizeTier (game, hits, value, reference_month) modela a faixa de premiacao por Jogo+quantidade de acertos, capturada mensalmente por update_monthly_prize_values, retendo so os 3 meses mais recentes por (game,hits). calculate_bet_prize passa a validar contra PrizeTier em vez do if/elif hardcoded. LotteryResult.prizes OU PrizeTier vigente sao as 2 fontes aceitas de valor (nunca estimativa). Novo FR-15 (fora do escopo original desta espinha, adicionado a PRD): LotteryResult nao tem purge automatico, admin ganha acao manual de purge por data. Deferred item sobre FR-8 removido - resolvido.
- (event) lint_spine.py OK apos adicionar AD-10
