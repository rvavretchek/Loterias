---
scope: Adendo 2026-09-17 do PRD (FR-16 a FR-24, UJ-3) - reorganizacao da home autenticada, Regras de Geracao personalizadas por usuario+Jogo (GenerationRule + reescrita de generate_bet), filtros cumulativos no historico
purpose: build-substrate
altitude: epic
updated: 2026-09-19T15:16
---

- (constraint) Spine pai: _bmad-output/planning-artifacts/architecture/architecture-Loterias-2026-09-08/ARCHITECTURE-SPINE.md - AD-1 a AD-10 herdados como vinculantes/read-only, nunca renumerados. Paradigma herdado: Django MVT monolith + Shared-Database batch integration (views/jobs so se comunicam via SQLite, nunca chamada direta).
- (constraint) AD-1 [herdado]: identificadores de codigo em ingles, inclusive novo. AD-3 [herdado]: novos models de dominio ficam em apps/loterias_core, nao um app novo. AD-4 [herdado]: cobertura de estado (nao de evento) e get_or_create pra evitar duplicidade - padrao relevante pro GenerationRule (unico por user+game, mesmo espirito de NotificationPreference).
- (constraint) AD-7 [herdado]: SQLite com timeout=20 (nao WAL), dois processos (web+cron) escrevendo no mesmo arquivo - generate_bet() roda no processo web (views), nunca no cron; GenerationRule e lido por generate_bet(), nunca escrito por jobs.py.
- (constraint) Convencao herdada (Consistency Conventions do spine pai): flags booleanas de preferencia sempre com default explicito; nenhum novo model de estado 'pendente' quando o valor unusable/None ja basta (AD-9 usa set_unusable_password() em vez de campo novo) - relevante pro design de default vs personalizado do GenerationRule.
- (decision) GenerationRule: 1 tabela relacional, 1 linha por (user, game, rule_name) - unique_together (user, game, rule_name), campos enabled (bool), numeric_value (int, nullable), choice_value (char, nullable, so pra tipo_distribuicao). Rejeitadas: 3 models por familia de jogo (duplicacao, mesmo padrao que a retro do Epic 2 flagou) e JSONField (primeiro do projeto, sem precedente, admin mostra blob cru). 'Mais recentemente alterada' (FR-22) cai de graca no updated_at de cada linha - especificamente a mais recente ENTRE as regras que estao causando o conflito na tentativa atual, nao a mais recente no geral (senao relaxar nao ajudaria a resolver o conflito).
- (decision) generate_bet(): extraida 1 funcao pura nova em utils.py, bet_satisfies_rules(numbers, clovers, game, rules) -> (ok, violated_rule_name|None), testavel isolada da geracao. generate_bet() continua orquestrando o loop de tentativas (mesmo padrao max_attempts ja usado em regenerate_bet_view), agora consultando GenerationRule pro user+game (ou caindo no default adaptativo de sequencia se nao existir linha nenhuma - AD-23) e chamando bet_satisfies_rules a cada candidato. Rejeitadas: tudo inline em generate_bet() (mistura geracao+validacao, dificil testar regra isolada) e uma camada de classes/motor de regras por familia (primeira abstracao OO do projeto, over-engineering pra ~15 regras).
- (decision) Mecanica exata do relaxamento (FR-22): generate_bet() roda ate max_attempts=1000 com o conjunto completo de regras ativas; se esgotar, identifica quais regras o ULTIMO candidato violou, escolhe a de maior updated_at entre essas, desliga so essa (efemero, so pra esta chamada - nunca escreve no banco) e roda MAIS max_attempts=1000 tentativas com o conjunto reduzido. Relaxa no maximo 1 regra por chamada de generate_bet() (FR-22 fala em 'a regra', singular). Se ainda assim falhar (caso extremo, nunca visto em teste manual mas teoricamente possivel com Totalmente Aleatoria + varias regras rigidas), cai no comportamento de hoje de 'Nao foi possivel gerar um jogo unico' (mesma mensagem ja usada em regenerate_bet_view) - nao tenta relaxar uma segunda regra.
- (decision) URL slug de /regras/<jogo>/: os valores de GAMES_CONFIG ja sao ASCII-safe (Mega-sena, Milionaria, Lotomania, Lotofacil, Quina, Dupla-Sena - sem acento, sem '+'), entao o slug e so o nome do jogo em lowercase com espacos->hifen (ja nao tem espaco). Nenhuma tabela de mapeamento nova necessaria.
- (decision) Views novas (edicao de GenerationRule, filtros de historico) entram em apps/loterias_core/views.py, seguindo a convencao existente (nenhum views/ package hoje) - AD herdado nao aborda isso explicitamente mas e a convencao real do codigo (investigado). Views.py cresce de 517 pra provavelmente ~650-700 linhas com esta epic - abaixo do limiar que preocuparia, mas o item de acao 'avaliar split' ja registrado na retro do Epic 2 continua valendo pro futuro.
- (decision) Filtro 'so premiados' do historico (FR-24): GeneratedBet.objects.filter(prize__gt=0) - reusa o campo cache ja existente (AD-4 do spine pai), sem join com HitNotification nem campo novo. Filtro por Jogo: filter(game=). Filtro por periodo: filter(created_at__range=(inicio,fim)) - data de geracao do GeneratedBet, nao data do sorteio (LotteryResult nao tem data de sorteio armazenada, so captured_at do momento em que o resultado foi capturado, que e um dado tecnico diferente e nao o que o usuario quer dizer com 'periodo').
- (question) PRD §8.5 (grid do volante oficial, FR-19/FR-20): pesquisa web parcial. CONFIRMADO com fonte - Mega-Sena: 6 linhas x 10 colunas (01-10, 11-20...51-60), grid regular todo numero -> linha=ceil(n/10), coluna=((n-1) mod 10)+1 (https://www.megaloterias.com.br/mega-sena/estatisticas/linhas-colunas). Lotofacil: grid 5x5 (01-25) (https://lottocap.com.br/blog/volante-da-lotofacil/). NAO CONFIRMADO - Quina (80 numeros), Milionaria/Dupla-Sena (50 numeros cada): buscas nao acharam a contagem oficial de colunas do volante fisico da Caixa (fontes genericas nao especificam). Continua como questao aberta pra essas 3 - nao adivinhar o grid, verificar direto com a Caixa ou um volante fisico antes de implementar FR-19 pra esses 3 jogos especificamente. Mega-Sena e Lotofacil podem prosseguir com o grid confirmado acima.
- (decision) PRD §8.9 (algoritmo de Distribuicao Homogenea): definido - divide numbers_count do Jogo em bets_count faixas de largura igual (ex. Mega-Sena: 6 apostas, 60 numeros -> 6 faixas de 10), sorteia exatamente 1 numero de cada faixa (fallback: se uma faixa ficar sem numero valido por causa de outra regra ativa, sorteia da faixa mais proxima disponivel, registrado como parte do mesmo loop de tentativas de generate_bet(), nao como uma regra separada que pode ser relaxada). 'Totalmente Aleatoria' = comportamento de hoje (amostragem uniforme sobre o intervalo inteiro, sem faixa). Baixo risco (fica contido dentro de bet_satisfies_rules/gerador, nao cria inconsistencia entre builders) mas resolvido aqui pra desbloquear a escrita de stories.
- (question) PRD §8.6 (faixa de valores validos por regra, ex. limite de sequencia entre 1 e quantos) permanece aberta - e tuning de produto especifico por regra/Jogo (ex. Mega-Sena tem 6 apostas, um limite de sequencia >=6 nao faz sentido; Lotofacil com 15-20 apostas tem faixa bem diferente), nao uma invariante de arquitetura. Nao bloqueia o desenho do model (numeric_value e um IntegerField generico, a validacao de faixa fica em forms.py/serializers, nao no schema). Decisao de produto pro Boss definir por story.
- (event) Reviewer gate: rubric walker e adversarial dispararam como subagentes; rubric walker falhou por rate limit da sessao (sem output), refeito inline pelo parent depois. Adversarial completou (14918 bytes escritos antes do erro de rate limit na etapa de resumo) - 8 achados reais (3 criticos, 2 altos, 2 medios, 1 baixo), arquivo em reviews/review-adversarial.md. Web-verification completou normalmente - 1 medio (AD-11 alegava 'sem precedente de JSONField no projeto', falso - GeneratedBet/LotteryResult/GameStatistics ja usam JSONField em 7 campos) e 1 baixo (AD-12 citava max_attempts=1000 de regenerate_bet_view como referencia, mas generate_bet() - a funcao real sendo modificada - ja tem seu proprio max_attempts=10000, inconsistencia de valor).
- (decision) Correcoes ao AD-12 (F1/F3 do adversarial + achado do web-verification): bet_satisfies_rules muda de retornar (bool, str|None) pra (bool, list[str]) - lista de TODAS as regras violadas pelo candidato, nao so a primeira. generate_bet() usa max_attempts=10000 (o mesmo ja existente na funcao, nao 1000 - regenerate_bet_view era so uma referencia de padrao, nao o valor a copiar). Duas queries distintas e nomeadas: 'active_rules' = GenerationRule.objects.filter(user=,game=,enabled=True) (alimenta bet_satisfies_rules) vs 'has_customization' = GenerationRule.objects.filter(user=,game=).exists() sem filtro de enabled (decide default vs personalizado, AD-13) - propositalmente diferentes.
- (decision) Correcao ao AD-13/AD-11 (F2 do adversarial): regras_geracao_view NUNCA deleta uma linha de GenerationRule ao desmarcar um toggle no formulario - sempre update_or_create com enabled=False. Salvar o formulario grava 1 linha por rule_name mostrado na tela daquele Jogo (todas, ligadas ou desligadas) - e assim que 'has_customization' (exists() sem filtro) reflete de verdade 'usuario configurou esse jogo'. O UNICO caminho que deleta linhas e o botao 'Restaurar padrao' do UX (GenerationRule.objects.filter(user=,game=).delete()) - acao explicita e distinta de desmarcar um toggle.
- (decision) Correcao ao AD-11 (F6/F7 do adversarial + achado do web-verification): remover a alegacao falsa de 'sem precedente de JSONField' - a razao real de usar tabela relacional em vez de JSON e o updated_at por linha (necessario pro relax do FR-22) e a visibilidade individual no Django admin, nao ausencia de precedente (JSONField ja e usado 7x no projeto pra dados array/dict, mas GenerationRule tem forma diferente - configuracoes nomeadas, nao uma lista/dict solto). Adicionado CheckConstraint no model garantindo XOR entre numeric_value/choice_value (nunca os dois nao-nulos na mesma linha). Adicionado RULE_NAMES_BY_GAME (dict Python em models.py, ao lado de GAMES_CONFIG) como fonte de verdade unica do enum de rule_name por familia de jogo - a Capability Map markdown vira documentacao derivada, nao fonte; Field.choices e toda validacao de view importam desse dict.
- (decision) Correcao a Capability Map (F4/F5 do adversarial): linhas de FR-19/FR-20/FR-21 reescritas como listas FECHADAS de rule_name por Jogo (nao mais notacao incremental '+'), com carve-out explicito: limit_row_count/limit_column_count validos hoje SO pra Mega-Sena (grid confirmado); Milionaria/Quina/Dupla-Sena da mesma familia FR-19 ficam sem essas 2 choices ate o grid ser confirmado (Deferred). Lotofacil (FR-20) declarado explicitamente SEM distribution_type (nao herda de FR-19 - decisao explicita, nao ambiguidade).
- (decision) Correcao ao AD-14 (F8 do adversarial): filtro de periodo do historico - created_at__range usa limites inclusivos, convertidos de date (input HTML type=date, sem hora) pro inicio do dia (00:00:00) e fim do dia (23:59:59) no TIME_ZONE do projeto - nao datas naive comparadas direto contra um DateTimeField timezone-aware.
- (event) Autocorrecao: ao aplicar o achado F5 (ambiguidade da notacao '+'), removi por engano distribution_type de Lotofacil na Capability Map - o PRD FR-20 (prd.md linha 343-354) lista distribution_type explicitamente como a 7a regra de Lotofacil, nao e heranca implicita de FR-19. Corrigido de volta - Lotofacil tem 7 rule_name, nao 6.
- (event) Reconciliacao de inputs (inline, dado o rate limit) - 1 item real: NFR de latencia do FR-22 (PRD §4.4) nao tinha resposta explicita no spine - adicionada nota no Stack explicando que o teto dobrado de tentativas (ate 20000) nao introduz espera perceptivel, ja que cada tentativa e trabalho em memoria sem I/O. Demais pontos checados (confirmacao FR-23 e botao Restaurar padrao) ja batiam entre PRD/UX/spine sem gap.
- (event) Reviewer gate completo: rubric walker (refeito inline apos falha do subagente por rate limit, reviews/review-rubric.md, confirmatorio - sem achado novo), web-verification (reviews/review-web-verification.md, 1 medio + 1 baixo, ambos aplicados), adversarial (reviews/review-adversarial.md, 3 criticos + 2 altos + 2 medios + 1 baixo, todos os 8 aplicados ao texto dos ADs). lint_spine.py limpo (0 findings) apos todas as correcoes.
- (event) spine finalized
- (change) 2026-09-18 (pos Story 4.3): Boss pediu as 5 regras (incl. linha/coluna) tambem pra Milionaria/Quina/Dupla-Sena - revertido o carve-out. RULE_NAMES_BY_GAME ganhou limit_row_count/limit_column_count nesses 3; GAME_GRID em models.py (Mega-sena 6x10 e Lotofacil 5x5 confirmados; Quina 8x10, Milionaria 5x10, Dupla-Sena 5x10 seguem o padrao de 10 colunas, SEM fonte - pendente confirmacao do Boss antes da 4.4 avaliar linha/coluna).
- (decision) 2026-09-19: Boss confirmou os grids do volante - Milionaria 5x10, Quina 8x10, Dupla-Sena 5x10. PRD 8.5 resolvido (GAME_GRID completo).
