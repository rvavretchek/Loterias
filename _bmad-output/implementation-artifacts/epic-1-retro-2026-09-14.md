---
epic: 1
date: 2026-09-14
verdict: accepted
criteria: profiled
headless: false
---

# Retrospectiva — Épico 1 (Renomeação de identificadores de código pra inglês)

## Resumo do épico

Épico 1 renomeou todo identificador de código (models, constantes, funções de domínio, views,
`admin.py`, `urls.py`, templates, campos remanescentes do model `User`) de `apps/loterias_core` e
`apps/accounts` de português pra inglês, preservando pt-br em strings de UI, `verbose_name`,
vocabulário de domínio e nomes de arquivo de template.

**Stories:** 1-1 (models/constantes), 1-2 (funções de utils.py), 1-3 (views/admin/urls/templates),
1-4 (reverse accessors + runbook de migration), 1-5 (campos `tema_preferido`/`telefone` do model
`User`). Todas com status `done` em `sprint-status.yaml` — nenhuma pendente (`pending_stories: []`,
confirmado via `sprint_status.py detect-epic --epic 1`).

**Diff range:** `50ab905^..68e4f27` (10 commits, sem merges). Evidência completa via
`git_evidence.py` — 36 arquivos de código/doc tocados, maior churn em `apps/loterias_core/tests.py`
(+499/-269) e `apps/loterias_core/views.py` (+197/-190), consistente com um épico de rename puro
(sem mudança de lógica).

**Evidência disponível:** epic spec (`_bmad-output/planning-artifacts/prds/.../prd.md` §3.1, mapa de
nomenclatura), 5 spec files por story, `sprint-status.yaml`, `deferred-work.md` (achados de revisão
de código já registrados por story durante o próprio épico). **Faltando:** logs de sessão individuais
das stories 1.1-1.5 não estão disponíveis nesta conversa — lições de processo ficam limitadas ao que
os specs/deferred-work já registraram.

## Achados

Nenhum achado novo de aggregate-view além do que a revisão de código de cada story já capturou.
Revisitando os 3 itens de `deferred-work.md` originados na revisão da Story 1.3 (única fonte de
achados cross-story deste épico) contra o estado atual do código:

1. **Nomes de arquivo de template continuam em português** (`historico.html`, `detalhes_jogo.html`,
   `estatisticas.html`) enquanto views/rotas já estão em inglês.
   — *Fonte:* `deferred-work.md` linhas 4-9 (achado do Blind Hunter na Story 1.3).
   — *Status atual:* ainda presente (confirmado, os 3 arquivos continuam com esse nome).
   — *Disposição:* **accept as-is** — decisão já registrada no spec como fora do escopo oficial do
     rename (nome de arquivo de template não é identificador de código Python, é string literal
     análoga a uma rota). Não é uma regressão do épico, é um limite de escopo já deliberado.

2. **`config.clovers`/`config.clovers_count` em `GAMES_CONFIG` sem comentário distinguindo os dois.**
   — *Fonte:* `deferred-work.md` linhas 11-13.
   — *Status atual:* ainda sem comentário (`apps/loterias_core/models.py`, dict `GAMES_CONFIG`).
   — *Disposição:* **defer** — risco de manutenção baixo, correção trivial (um comentário), mas não
     bloqueia o épico. Vira action item.

3. **PROMOVIDO para Stories 2.13/2.14** (validação de jogo em `api_create_bet_view`, bloqueio real de
   concurso duplicado) — já tratado como trabalho futuro fora deste épico, não é um achado do Épico 1
   em si.

Nenhum achado novo de divergência spec-vs-implementação: o mapeamento antigo→novo documentado na PRD
§3.1 bate com o estado atual do código (verificado por amostragem em `models.py`, `utils.py`,
`views.py` — todos os identificadores relevantes já em inglês).

## Verificação de comportamento

Suíte de testes completa rodada nesta retrospectiva (`.venv` com Python 3.11/Django 5.0.6, o
interpretador pinado — o interpretador default da máquina é Python 3.14/Django 6.0.5 e quebra o
model `NotificationPreference`, confirmando o aviso já existente no CLAUDE.md sobre desalinhamento de
ambiente): **281 testes, OK**, cobrindo `apps.loterias_core` e `apps.accounts`. Épico 1 foi um rename
puro (sem mudança de comportamento pretendida) — a suíte verde é a evidência correta de que nenhuma
regressão funcional foi introduzida pelas 5 stories.

## Previous-retro follow-through

Nenhuma retrospectiva anterior existe pra este projeto (`epic-1-retro-2026-09-14.md` é a primeira) —
nada a verificar aqui.

## Action items

1. **Adicionar comentário em `GAMES_CONFIG` (models.py) distinguindo `clovers` de `clovers_count`.**
   Owner: próxima sessão de dev que tocar `GAMES_CONFIG`. Fix trivial, baixo risco, sem story
   dedicada necessária — pode ser feito juntamente com qualquer story futura que já mexa em
   `models.py`.

Nenhuma lição de processo nova: o épico seguiu exatamente o que a PRD já havia planejado (mapeamento
de nomenclatura §3.1), sem desvio.

## Veredito de aceitação

**Accepted.** Critérios de aceitação profilados a partir do diff/PRD (nenhum critério formal
declarado separadamente no epic spec): (1) todo identificador de código em
`apps/loterias_core`/`apps/accounts` em inglês — confirmado; (2) strings de UI/domínio/pt-br
preservadas — confirmado; (3) nenhuma regressão funcional — confirmado via suíte de testes (281 OK).
Nenhuma story pendente. Nenhum achado bloqueante em aberto (o único item aberto é um comentário de
manutenção de baixíssimo risco, não um bug).

## Questões em aberto

Nenhuma questão que mudaria o veredito. O único ponto de atenção genuinamente aberto (nomes de
template em português) já tem decisão registrada como aceita — não precisa de nova decisão do Boss
a menos que ele queira reabrir esse escopo.
