---
epic: 3
date: 2026-09-14
verdict: accepted
criteria: profiled
headless: false
---

# Retrospectiva — Épico 3 (Novo fluxo de cadastro só-com-e-mail)

## Resumo do épico

Épico 3 reconstruiu o fluxo de cadastro: usuário se cadastra só com e-mail, confirma por link,
define senha via link de definição, com tratamento de vínculo expirado e nome/sobrenome obrigatórios
no primeiro login (`ProfileCompletionView`/middleware).

**Stories:** 3-1 (cadastro inicial só-com-e-mail), 3-2 (reenvio do e-mail de confirmação), 3-3
(definição de senha via link), 3-4 (vínculo expirado), 3-5 (nome/sobrenome obrigatórios no primeiro
login). Todas `done` em `sprint-status.yaml`, nenhuma pendente.

**Diff range:** `48d5150^..edf3327` (5 commits). Escopo real do épico: `a68e6dd` (implementação das 5
stories, um único commit) + `edf3327` (10 achados de revisão corrigidos). Os outros 3 commits no meio
do range (`abf5e21` fix de env do cron, `be48750` doc do runbook de deploy, `d8781d3` promoção de
itens do deferred-work) **não são trabalho do Épico 3** — ficaram intercalados na mesma sessão de
desenvolvimento por conveniência; `deploy/lab/docker-compose.yml`, `Dockerfile` e
`deploy/lab/README.md` no diff de arquivos vêm desses commits, não do cadastro. Tratados como fora de
escopo pra esta retrospectiva.

**Evidência disponível:** `spec-epic-3-novo-fluxo-de-cadastro.md`, `sprint-status.yaml`,
`deferred-work.md` (1 item tagueado a este épico), a mensagem do commit `edf3327` (lista completa dos
10 achados de uma revisão de 4 camadas — Blind Hunter, Edge Case Hunter, Verification Gap Reviewer,
Acceptance Auditor — já pedida pelo Boss especificamente em preparação pra esta retrospectiva).
**Faltando:** logs de sessão individuais.

## Achados

### Diff-scope review — já coberta antes desta retrospectiva

O Boss já encomendou uma revisão de código de 4 camadas sobre `a68e6dd` especificamente "antes da
retrospectiva do épico" (mensagem do commit `edf3327`), cobrindo exatamente o que Fase 2 desta skill
pediria (Blind Hunter/Edge Case Hunter ≈ adversarial + edge-case; Verification Gap Reviewer ≈
verification-gap; Acceptance Auditor é um lens adicional específico deste projeto). Repetir a revisão
do zero seria redundante — em vez disso, verifiquei por amostragem que os 10 achados corrigidos
continuam de fato presentes no código atual (não regrediram):

- `ConfirmEmailView` customizada existe (`apps/accounts/views.py:34`) — corrige o `?next=` pulando
  criação de senha obrigatória.
- `User.profile_completed` com `default=True` no model (`models.py:52`) + `UserManager.create_user`
  força `True` explicitamente (`models.py:17`) + `adapter.py` marca `False` só no caminho público de
  cadastro (`adapter.py:31`, comentário AD-8) — corrige o bypass via Django admin.
- Demais achados (cooldown de reenvio, contador atômico, middleware pra JSON, guarda de
  double-submit, cap de 30 caracteres no form, testes reforçados) não re-verificados individualmente
  nesta retrospectiva, mas a suíte completa (ver Verificação de comportamento) os cobre
  transitivamente — qualquer regressão nesses pontos apareceria como teste quebrado.
- *Disposição:* **accept as-is** (já corrigido e testado antes desta retrospectiva).

### Achado adicional (deferred-work.md, ainda não corrigido)

**`createcachetable` não documentado no setup local do CLAUDE.md nem no runbook do lab.** O novo
`CACHES` (`DatabaseCache`, usado por `ResendConfirmationEmailView` pro cooldown/contador de reenvio)
exige essa tabela; um dev novo seguindo só o CLAUDE.md quebra no primeiro uso de cache. O
`Dockerfile`/deploy do lab já rodam `createcachetable` corretamente — só a documentação de dev local
ficou pra trás.
— *Fonte:* `deferred-work.md` linhas 117-122 (achado do Blind Hunter e Edge Case Hunter,
  independentemente, na mesma revisão de 4 camadas).
— *Disposição:* **fix now** — correção trivial (uma linha no bloco de comandos do CLAUDE.md), vira
  action item desta retrospectiva em vez de ficar deferida de novo.

Nenhum achado novo de divergência spec-vs-implementação além do já registrado.

## Verificação de comportamento

Suíte de testes completa rodada nesta retrospectiva (`.venv`, Python 3.11/Django 5.0.6 — o mesmo
ambiente pinado usado na retro do Épico 1): **281 testes, OK**, incluindo `apps.accounts` (fluxo de
cadastro/confirmação/definição de senha/perfil). Mesmo número reportado no commit `edf3327`
(281/281) — nenhuma regressão desde a correção dos 10 achados.

## Previous-retro follow-through

Único item aberto de uma retrospectiva anterior é o desta mesma sessão (Épico 1, item
`epic-1-retro-item-1-adicionar-comentario-em-games_config-mod`, comentário em `GAMES_CONFIG`) — não
pertence ao Épico 3 e não foi endereçado por este épico. Continua `open`, sem mudança proposta aqui.

## Action items

1. **Documentar `createcachetable` no bloco de comandos de setup local do CLAUDE.md** (e, se fizer
   sentido, no runbook do lab também, embora o Dockerfile já rode). Owner: próxima sessão de dev.
   Evita que um dev novo quebre no primeiro uso de `ResendConfirmationEmailView`.

Nenhuma lição de processo nova além da já demonstrada nesta sessão: pedir uma revisão de código
dedicada *antes* da retrospectiva (como o Boss já fez aqui) evita que a retrospectiva repita trabalho
de revisão do zero — vale manter como prática pros próximos épicos.

## Veredito de aceitação

**Accepted.** Critérios profilados a partir do spec do épico (nenhum critério formal separado
declarado): (1) cadastro só-com-e-mail funcional com confirmação por link — confirmado; (2) definição
de senha via link, sem bypass via `?next=` — confirmado e testado; (3) vínculo expirado tratado —
story 3.4 done, coberta pela suíte; (4) nome/sobrenome obrigatórios no primeiro login, sem
double-submit — confirmado; (5) nenhuma regressão — 281/281 testes. Nenhuma story pendente. O único
achado em aberto (documentação do `createcachetable`) é um gap de documentação de baixo risco, não um
bug de comportamento — não bloqueia o veredito.

## Questões em aberto

Nenhuma questão que mudaria o veredito.
